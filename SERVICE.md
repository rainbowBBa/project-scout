# SERVICE · project-scout (기술스택 조사 에이전트)

## 0. 한눈에

**만들고 싶은 소프트웨어를 한 줄로 설명하면, 어떻게 만들어야 하는지 근거와 함께 답하는 Agent.**

``` bash
사용자 질문: "AI 요약 기능이 있는 팀 채팅 앱"
        ↓
LLM 진행: 필요한 요소 뽑기 → 요소마다 후보 찾기 → 후보마다 사실 수집 → 판정 → 점수·순위
        ↓
리포트 생성: report.html  (근거 URL과 조회 날짜가 붙은 스택 결정서)
```

- 사실 수집은 **코드**가, 판단만 **LLM**이 한다
- LLM은 코드가 모아둔 사실만 인용할 수 있고, 그 인용이 실제로 있는지 코드가 다시 검사한다
- 그래서 "왜 이걸 골랐나"에 6개월 뒤에도 답할 수 있다

## 1. 사용자·문제·가치

- 누구를 위한 서비스인가: 새 프로젝트의 기술 스택을 정해야 하는 개발자, 그 결정을 검토하는 팀 리드
- 어떤 문제를 푸는가: 후보 조사·비교에 며칠이 걸리고, 정한 뒤에도 근거가 남지 않는다
- 지금은 어떻게 해결하나: 구글링 + 블로그 + GitHub 별 개수, 또는 LLM에 물어보기(근거 없음)
- 이걸로 뭐가 좋아지나: 근거가 붙은 스택 결정서가 HTML 한 장으로 나온다
- 남다른 점: **"지금 만들지 않아도 되는 것"을 먼저 알려준다** — 안 만든 기능이 가장 큰 절약이다

## 2. 서비스 확장 관점

- 사내 기여: "무엇을 확인해야 하는가"라는 판단 기준을 도구가 대신 기억한다
- 규모: 1인 사용으로 시작해 사내 다른 팀으로 넓힌다. 조사 규모는 플래그로만 조절한다
- 대체제와 차별점: LLM은 학습 시점 이후를 모르고 근거를 못 낸다. 검색 엔진은 종합을 안 해준다
- 확장 경로: 사내 검색 API 교체 · 사내 승인 패키지 목록 추가 · MCP 서버를 DMZ로 분리
- 역방향 사용: 기존 프로젝트의 `package.json`을 넣으면 같은 파이프라인이 스택 진단 도구가 된다

## 3. 사용 예상 도구·데이터

- 실행: `uv run scout` → 설명 입력 → 되묻기 몇 번 → `runs/<slug>/report.html`
- 6단계: `interview`(요청 구체화) → `design`(구현 설계 + 결정 지점) → `search`(후보·사실 수집) → `verify`(판정) → `evaluate`(점수·순위 + 설계 확정) → `report`(HTML)
- 도구: npm · PyPI · GitHub · OSV(취약점) · 웹 검색 — 전부 별도 MCP 서버를 통한다 (인터넷 출구는 여기 하나)
- 데이터: 후보별 사실 자료철(dossier). 각 사실은 `npm.last_release`처럼 id를 갖는다
- 저장: 실행 결과 전부가 `scout.db`(sqlite) 한 파일에 남는다 — 앞 단계를 다시 돌리지 않고 재실행 가능
- 스택: AWS Bedrock `claude-sonnet-5` · LangGraph · MCP · sqlite3 · jinja2 · uv 워크스페이스

## 4. 서비스 정책 (가드레일 요약)

- 없는 사실을 인용하면 코드가 잡아낸다 — 지어낸 근거로는 판정이 통과하지 못한다
- 성숙도·위험도는 코드가 계산한다. LLM 점수는 하나뿐이고, 계산값의 평균이 아니다
- 근거가 없으면 "낮음"이 아니라 **"없음"**으로 표시한다
- 조회 실패는 파이프라인을 멈추지 않는다 — 못 구했다고 기록하고 계속한다
- 빈 섹션을 지우지 않는다. "해당 없음 + 이유"를 쓴다
- 조사하지 않은 것과 **조사할 필요가 없다고 판단한 것**을 구분해서 보여준다
- 보고서는 LLM을 쓰지 않는다. DB를 그대로 렌더링만 한다
- 크레덴셜은 로그·보고서에 찍지 않고, 인터넷 담당 프로세스에 AWS 키를 넘기지 않는다

## 5. 성공 기준

- 한 줄 설명으로 끝까지 완주해 `report.html`이 나온다
- **내가 몰랐던 사실이 최소 1건** 나온다 — 다 아는 걸 확인해주기만 하면 값이 없다
- 걸러진 요소(`defer`/`unnecessary`)가 0개가 아니고, 그 판단이 타당하다
- 테스트 4종 통과 — 인용 검증 · 낡은 사실 반영 · 걸러내기 배선 · 인터넷 출구 통제
- 나 외에 최소 1명이 자기 프로젝트에 쓰고, 보고서가 실제 의사결정 문서에 인용된다
- 실패 신호: 점수가 늘 비슷하다 · 걸러진 요소가 늘 0개 · 결과가 "당연한 것"만 나온다
