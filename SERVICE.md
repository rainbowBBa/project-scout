# project-scout — 서비스 정의

> 무엇을 왜 만드는가 · 사용자 · 문제 · 가치 · 확장 · 성공 기준

기술 설계는 [docs/001](docs/001_기술스택-조사-에이전트-설계/README.md) ·
개발 순서는 [docs/002](docs/002_개발계획/README.md)

---

## 무엇을 왜 만드는가

**만들고 싶은 소프트웨어를 한 줄로 설명하면, 어떻게 만들어야 하는지 근거와 함께 답하는 도구.**

새 프로젝트를 시작할 때마다 같은 일을 반복한다. 필요한 요소를 나열하고, 각각을 어떻게
구현할지 찾아보고, 후보를 비교하고, 고른 이유를 정리한다. 며칠이 걸리고, 그렇게 정한 뒤에도
반년 뒤에 "왜 이걸 골랐지"에 답할 수 없다.

LLM에 물어보면 10초에 답이 나온다. 그런데 그 답을 믿을 근거가 없다.

**이 도구는 그 사이를 메운다** — LLM의 속도로 답을 내지만, 판단의 재료를 코드가 먼저
수집하고 그 인용을 코드가 검증한다. 결과는 근거가 붙은 결정이고, 6개월 뒤에도 추적된다.

---

## 사용자

### 1차 — 새 프로젝트를 시작하는 개발자

스택을 정해야 하는 사람. "실시간 채팅 기능이 필요한데 뭘 써야 하지"에서 출발해
**하루 안에 근거 있는 결정에 도달하고 싶은** 사람.

이 사람이 지금 하는 일: 구글링 → 블로그 3개 → GitHub 별 개수 확인 → 대충 유명한 걸 고름.
빠진 것: 마지막 커밋일, 취약점, 라이선스, 그리고 **애초에 그 기능이 필요한지**.

### 2차 — 그 결정을 검토·승인하는 팀 리드

보고서를 받는 사람. 결정 자체보다 **"왜 그렇게 정했나"와 "무엇을 안 하기로 했나"**가 궁금하다.
지금은 그 답이 구두로만 존재한다.

보고서를 단일 HTML로 만든 이유가 이 사람이다 — 파일 하나 첨부하면 끝나야 한다.

### 잠재 — 같은 판단을 반복하는 사내 다른 팀

한 사람이 축적한 판단 기준(무엇을 확인해야 하는가)을 도구가 대신 기억한다.
사내 승인 패키지 목록 같은 조직 고유 제약을 넣으면 외부 도구가 대체할 수 없는 도구가 된다.

---

## 문제

### 1. 결정에 며칠이 걸린다

요소 하나당 후보 3개를 조사하면 하루가 간다. 요소가 8개면 일주일이다.
그런데 이 조사의 대부분은 **기계가 할 수 있는 일**이다 — 릴리스 날짜 확인, 커밋 이력 조회,
취약점 검색.

### 2. LLM에 물으면 빠르지만 근거가 없다

- 학습 시점 이후의 생태계 변화를 모른다
- **별 2만 개짜리 라이브러리가 18개월간 커밋이 없다는 걸 말해주지 않는다**
- 사용자의 예산 · 팀 · 데드라인이라는 평가 기준이 없다 —
  "실시간 채팅"만 주면 Firebase도 정답이고 Kafka도 정답이다

### 3. 안 만들어도 되는 걸 만든다

**가장 비싼 문제다.** 도구들은 "이것도 필요하고 저것도 필요하다"만 말한다.
200명 사내 채팅에 전문검색 엔진이 정말 필요한가? `LIKE` 쿼리로 충분한 걸
Elasticsearch로 만들고 나면 운영 부담이 영구히 남는다.

### 4. 결정의 근거가 남지 않는다

3개월 뒤 "왜 socket.io였지"에 답할 수 없다. 새 팀원이 물으면 "그때 그게 제일 좋아 보여서"가
전부다. 되돌릴 근거도, 유지할 근거도 없다.

### 5. 판단과 사실이 섞여 있다

"성능이 좋다"(판단)와 "마지막 릴리스가 11일 전"(사실)이 같은 문장에 섞여 나온다.
읽는 사람이 어디까지 믿어야 할지 알 수 없다.

---

## 가치

| 문제 | 이 도구가 주는 것 |
|---|---|
| 며칠이 걸린다 | **1회 실행 10분 내외.** 조사 가능한 부분은 코드가 병렬로 처리 |
| 근거가 없다 | 모든 판정에 `fact_id` + URL + 조회 날짜. 6개월 뒤에도 추적된다 |
| 안 만들어도 되는 걸 만든다 | **"지금 만들지 않아도 되는 것" 섹션.** 걸러낸 이유까지 |
| 결정 근거가 안 남는다 | 단일 HTML 보고서 — 탈락 사유 · 근거 목록 · 명시된 가정 포함 |
| 판단과 사실이 섞인다 | `computed` / `judged` / `근거 없음` 배지로 행마다 구분 |

### 가장 차별적인 것 두 가지

**1. 안 만들어도 되는 걸 알려준다.**
다른 도구는 무엇을 쓸지만 말한다. 이 도구는 **무엇을 안 해도 되는지**를 먼저 판단한다.
안 만든 기능이 가장 큰 절약이다.
걸러낸 요소가 실제로 걸러지는지는 `test_necessity_wiring.py`가 자동으로 검사한다 —
걸러놓고 다 조사하면 기능이 장식이 되기 때문이다.

**2. LLM이 사실을 지어낼 수 없다는 구조적 보증.**
판정을 내리는 LLM은 코드가 미리 수집한 사실 목록에서 **id로만** 인용할 수 있고,
그 인용이 실제로 존재하는지 코드가 다시 대조한다.
"믿어주세요"가 아니라 검증 가능한 구조다 —
[grounding](docs/001_기술스택-조사-에이전트-설계/stages/3-verify.md) 참조.

### 가치가 없는 경우 (정직하게)

- **이미 스택이 정해진 프로젝트** — 조사할 게 없다
- **아주 작은 프로젝트** — 요소가 2개면 직접 찾는 게 빠르다
- **매우 새로운 영역** — 레지스트리에 데이터가 없으면 근거가 웹 스니펫뿐이다
- **판단이 조직 정치인 경우** — "부장님이 그거 쓰라고 했다"는 도구가 못 푼다

---

## 확장 관점

프로토타입은 의도적으로 작다. 확장 경로가 **설계에 미리 심겨 있다.**

### 즉시 (플래그만)

| 확장 | 방법 |
|---|---|
| 조사 규모 | `--max-components 3` → `8`, `--max-candidates 3` → `5` |
| 판정 품질 | pointwise 1회 → 다수결 3회 (상수만 변경) |

### 사내 도구화 (설계에 심어둔 경로)

| 확장 | 준비된 것 |
|---|---|
| **사내 검색 API로 교체** | `providers/search.py` 한 함수만 갈아끼운다. `SCOUT_SEARCH_PROVIDER` 환경변수로 전환 |
| **MCP 서버를 DMZ로 분리** | 워크스페이스가 이미 분리돼 있다. stdio → streamable-http 전환 한 줄. 크레덴셜 경계가 논리적 → **물리적**으로 승격 |
| **사내 승인 패키지 목록을 사실로 추가** | provider를 하나 더 붙이면 dossier에 들어간다. **외부 도구가 못 하는 판단**이 여기서 생긴다 |
| 웹 서비스화 | 보고서가 이미 self-contained HTML이다 |

### 도메인 확장

지금은 웹/SaaS + AI 앱. `analyze` 프롬프트의 **도메인 힌트 문자열**을 교체하면
게임 · 임베디드 · 데이터 파이프라인으로 넓어진다. 파이프라인 구조는 그대로다.

### 사실 소스 확장

절단선에서 뺀 것들이 대기 중이다 — `osv`(취약점) · `deps.dev`(전이 의존성) ·
npm 다운로드 추이. **provider 추가는 파이프라인을 건드리지 않는다.**

### 역방향 사용 — 예상 밖의 확장

`analyze`를 건너뛰고 **기존 프로젝트의 `package.json`을 입력으로** 받으면,
같은 파이프라인이 진단 도구가 된다.

> "당신 스택 중 3년째 커밋 없는 게 2개, 알려진 취약점이 있는 게 1개 있습니다."

`search`·`verify`·`evaluate`·`report`를 그대로 재사용한다.
새 기능처럼 보이지만 실은 입력만 바꾼 것이다.

---

## 성공 기준

기술 검증(테스트·E2E)은 [docs/001/07-검증](docs/001_기술스택-조사-에이전트-설계/07-검증.md)에 있다.
여기는 **서비스로서 성공했는지**를 본다.

### 프로토타입 (2일 뒤)

- [ ] **내가 다음 프로젝트를 시작할 때 실제로 이걸 돌린다.** 안 돌리면 실패다
- [ ] **적어도 하나, 내가 몰랐던 사실이 나온다.**
      다 아는 걸 확인해주기만 하면 값이 없다 — 이게 가장 중요한 기준
- [ ] 보고서를 팀에 공유했을 때 "이거 쓸만하네"를 듣는다
- [ ] `unnecessary` / `defer`로 걸러진 요소가 **0개가 아니고, 그 판단이 타당하다**
      (배선은 `test_necessity_wiring.py`가 자동 검사. 여기서 보는 건 판단 품질)

### 사내 도구 (1~2개월)

- [ ] 나 외에 **최소 1명**이 자기 프로젝트에 쓴다
- [ ] 보고서가 실제 의사결정 문서에 인용된다 (PR 설명 · 기획서 · 회의록)
- [ ] "지금 만들지 않아도 되는 것" 섹션이 **실제로 범위를 줄인 사례 1건**
- [ ] 사내 검색 API 또는 승인 패키지 목록이 연결된다 (외부 도구가 못 하는 판단의 시작)

### 실패 신호 — 이게 보이면 방향을 고친다

| 신호 | 무엇이 잘못됐나 |
|---|---|
| judge 점수가 늘 4~5만 나온다 | 관대함 편향 미해결. 앵커 루브릭이 약하다 |
| `unnecessary`/`defer`가 늘 0개 | 필요성 판단이 장식이다. 가장 값어치 있는 기능이 죽었다 |
| 결과가 "당연한 것"만 나온다 | 조사 깊이가 얕다. 규모 플래그를 올려야 한다 |
| 보고서를 만들고 아무도 안 읽는다 | 1페이지 요약이 결정에 필요한 걸 담지 못한다 |
| grounding 위반이 자주 뜬다 | judge 프롬프트가 dossier 범위를 명확히 못 전달한다 |
| 매번 결과가 다르다 | 판정 일관성 부족. 다수결(3회)이 필요한 시점이다 |

**실패 신호를 미리 적어두는 이유**: 2일 뒤에 "잘 됐다"고 느끼기는 쉽다.
무엇이 보이면 안 되는지를 지금 정해두면 그때 자기를 속이지 않는다.
